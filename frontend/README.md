# Clipdrop frontend

Deploy this folder to Netlify, Vercel, or Cloudflare Pages.

Before deploying, set `static/config.js` to the public backend URL:

```javascript
window.DOWNLOADER_API_URL = "https://your-backend.example.com";
```
